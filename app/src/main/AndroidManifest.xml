<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bookreviews">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.ShowAllBooks"/>
        <activity android:name=".ui.SearchBook"/>
        <activity android:name=".ui.AddBook"/>
        <activity android:name=".ui.ShowBook"/>
        <activity android:name=".ui.ShowAllReviews"/>
        <activity android:name=".ui.ShowReview"/>
        <activity android:name=".ui.AddReview"/>
        <activity android:name=".ui.ChangeSettings"/>
        <activity android:name=".ui.About"/>


    </application>

</manifest>